<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper PUBLIC   
    "-//mybatis.org//DTD Mapper 3.0//EN"  
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.markbro.dzd.cms.resourcetype.dao.ResourcetypeMapper">
	<!--<sql id="addColumns">id,name,parentid,parentids,code,description,createTime,updateTime,createBy,updateBy,available,deleted</sql> -->
	<!--<sql id="updateColumns">name,parentid,parentids,code,description,updateTime,createBy,updateBy,available,deleted</sql> -->
	<cache type="com.markbro.asoiaf.mybatis.cache.AsoiafCache" eviction="org.apache.ibatis.cache.decorators.LruCache" readOnly="true" size="1024" flushInterval="3600000"/>
	<sql id="allColumns">id,name,parentid,parentids,code,sort,description,createTime,updateTime,createBy,updateBy,available,deleted</sql>
	<!--如果实体类中属性名与数据库表中的字段名不一致就需要用resultMap映射-->
	<resultMap id="resourcetypeResult" type="com.markbro.dzd.cms.resourcetype.bean.Resourcetype">
		<result property="id" column="id" />
		<result property="name" column="name" />
		<result property="parentid" column="parentid" />
		<result property="parentids" column="parentids" />
		<result property="code" column="code" />
		<result property="sort" column="sort" />
		<result property="description" column="description" />
		<result property="createTime" column="createTime" />
		<result property="updateTime" column="updateTime" />
		<result property="createBy" column="createBy" />
		<result property="updateBy" column="updateBy" />
		<result property="available" column="available" />
		<result property="deleted" column="deleted" />
	</resultMap>
    <sql id="Resourcetype_Where_Clause">
        <if test="id != null and id != ''">
				AND id = #{id}
		</if>
			<if test="name != null and name != ''">
				AND name = #{name}
		</if>
			<if test="parentid != null and parentid != ''">
				AND parentid = #{parentid}
		</if>
			<if test="parentids != null and parentids != ''">
				AND parentids = #{parentids}
		</if>
			<if test="code != null and code != ''">
				AND code = #{code}
		</if>
		<if test="sort != null and sort != ''">
			AND sort = #{sort}
		</if>
			<if test="description != null and description != ''">
				AND description = #{description}
		</if>
			<if test="createTime_start != null and createTime_start != ''">
				<![CDATA[   and DATE_FORMAT(createTime, '%Y-%m-%d')>=  DATE_FORMAT(#{createTime_start}, '%Y-%m-%d')   ]]>
		</if>
			<if test="createTime_end != null and createTime_end != ''">
				<![CDATA[   and DATE_FORMAT(createTime, '%Y-%m-%d')<  ADDDATE(DATE_FORMAT(#{createTime_end}, '%Y-%m-%d'),1)   ]]>
		</if>
			<if test="updateTime != null and updateTime != ''">
				AND updateTime = #{updateTime}
		</if>
			<if test="createBy != null and createBy != ''">
				AND createBy = #{createBy}
		</if>
			<if test="updateBy != null and updateBy != ''">
				AND updateBy = #{updateBy}
		</if>
			<if test="available != null and available != ''">
				AND available = #{available}
		</if>
			<if test="deleted != null and deleted != ''">
				AND deleted = #{deleted}
		</if>
			
    </sql>
    <select id="get"  resultMap="resourcetypeResult">
        select <include refid="allColumns"/>
        from cms_resourcetype
        where
        id =#{id}
    </select>
    <select id="getMap"  resultType="java.util.Map">
            select a.*,b.name parentname
            from cms_resourcetype a,cms_resourcetype b
            where
			a.parentid=b.id
			and
            a.id =#{id}
    </select>
    <insert id="add" useGeneratedKeys="true" keyProperty="id" parameterType="com.markbro.dzd.cms.resourcetype.bean.Resourcetype" flushCache="true">
    	insert into cms_resourcetype(id,name,parentid,parentids,code,sort,description,createTime,updateTime,createBy,updateBy,available,deleted)
    	values (#{id},#{name},#{parentid},#{parentids},#{code},#{sort},#{description},now(),now(),#{createBy},#{updateBy},1,0)
    </insert>
    <insert id="addByMap" useGeneratedKeys="true" keyProperty="id" parameterType="java.util.Map" flushCache="true">
    	insert into cms_resourcetype(id,name,parentid,parentids,code,description,createTime,updateTime,createBy,updateBy,available,deleted)
    	values (#{id},#{name},#{parentid},#{parentids},#{code},#{description},now(),now(),#{createBy},#{updateBy},1,0)
    </insert>
    <insert id="addBatch" flushCache="true">
        insert into cms_resourcetype (id,name,parentid,parentids,code,description,createTime,updateTime,createBy,updateBy,available,deleted)
        values
        <foreach collection="list" item="item" separator="," >
        	(#{item.id},#{item.name},#{item.parentid},#{item.parentids},#{item.code},#{item.description},now(),now(),#{item.createBy},#{item.updateBy},1,0)
        </foreach>
    </insert>
    <update id="update" parameterType="com.markbro.dzd.cms.resourcetype.bean.Resourcetype" flushCache="true">
    	update cms_resourcetype 
    	<set>
    		<if test="name != null">name=#{name},</if>
			<if test="parentid != null">parentid=#{parentid},</if>
			<if test="parentids != null">parentids=#{parentids},</if>
			<if test="code != null">code=#{code},</if>
			<if test="sort != null">sort=#{sort},</if>
			<if test="description != null">description=#{description},</if>
			updateTime=now(),
			<if test="createBy != null">createBy=#{createBy},</if>
			<if test="updateBy != null">updateBy=#{updateBy},</if>
			<if test="available != null">available=#{available},</if>
			<if test="deleted != null">deleted=#{deleted},</if>
			
    	</set>
    	 where
    	id =#{id}
    </update>
	<update id="updateByMap" parameterType="map" flushCache="true">
        update cms_resourcetype
        <set>
            <if test="name != null">name=#{name},</if>
			<if test="parentid != null">parentid=#{parentid},</if>
			<if test="parentids != null">parentids=#{parentids},</if>
			<if test="code != null">code=#{code},</if>
			<if test="description != null">description=#{description},</if>
			updateTime=now(),
			<if test="createBy != null">createBy=#{createBy},</if>
			<if test="updateBy != null">updateBy=#{updateBy},</if>
			<if test="available != null">available=#{available},</if>
			<if test="deleted != null">deleted=#{deleted},</if>
			
        </set>
        where
        id =#{id}
    </update>
	<update id="updateByMapBatch" parameterType="map" flushCache="true">
         update cms_resourcetype
         <set>
         <if test="name != null">name=#{name},</if>
			<if test="parentid != null">parentid=#{parentid},</if>
			<if test="parentids != null">parentids=#{parentids},</if>
			<if test="code != null">code=#{code},</if>
			<if test="description != null">description=#{description},</if>
			updateTime=now(),
			<if test="createBy != null">createBy=#{createBy},</if>
			<if test="updateBy != null">updateBy=#{updateBy},</if>
			<if test="available != null">available=#{available},</if>
			<if test="deleted != null">deleted=#{deleted},</if>
			
         </set>
         where
         id in
         <foreach collection="ids" item = "id" open="(" separator="," close=")">
         #{id}
         </foreach>
    </update>
  	<delete id="delete" flushCache="true">
      	delete  from cms_resourcetype where id =#{id}
    </delete>
  	<delete id="deleteBatch" flushCache="true">
      	delete  from cms_resourcetype where id in
        <foreach collection="array" item = "ids" open="(" separator="," close=")">
        #{ids}
        </foreach>
    </delete>
    <select id="find" resultMap="resourcetypeResult" parameterType="java.util.Map">
        SELECT <include refid="allColumns"/> FROM cms_resourcetype
        <where>
            and available=1
            and deleted=0
            <include refid="Resourcetype_Where_Clause"/>
        </where>
		order by sort
    </select>
    <select id="findByMap" resultMap="resourcetypeResult"  parameterType="map" >
   		SELECT <include refid="allColumns"/> FROM cms_resourcetype
   		<where>
   		    <if test="id != null and id != ''">
				AND id = #{id}
			</if>
			<if test="name != null and name != ''">
				AND name = #{name}
			</if>
			<if test="parentid != null and parentid != ''">
				AND parentid = #{parentid}
			</if>
			<if test="parentids != null and parentids != ''">
				AND parentids = #{parentids}
			</if>
			<if test="code != null and code != ''">
				AND code = #{code}
			</if>
			<if test="description != null and description != ''">
				AND description = #{description}
			</if>
			<if test="createTime_start != null and createTime_start != ''">
				<![CDATA[   and DATE_FORMAT(createTime, '%Y-%m-%d')>=  DATE_FORMAT(#{createTime_start}, '%Y-%m-%d')   ]]>
			</if>
			<if test="createTime_end != null and createTime_end != ''">
				<![CDATA[   and DATE_FORMAT(createTime, '%Y-%m-%d')<  ADDDATE(DATE_FORMAT(#{createTime_end}, '%Y-%m-%d'),1)   ]]>
			</if>
			<if test="updateTime != null and updateTime != ''">
				AND updateTime = #{updateTime}
			</if>
			<if test="createBy != null and createBy != ''">
				AND createBy = #{createBy}
			</if>
			<if test="updateBy != null and updateBy != ''">
				AND updateBy = #{updateBy}
			</if>
			<if test="available != null and available != ''">
				AND available = #{available}
			</if>
			<if test="deleted != null and deleted != ''">
				AND deleted = #{deleted}
			</if>
   		</where>
		order by sort
   	</select>

	<select id="findByParentid" resultType="com.markbro.dzd.cms.resourcetype.bean.Resourcetype">
		select *
			from cms_resourcetype
		<where>
			parentid=#{parentid}
			and deleted=0
			and available=1
		</where>
		order by sort
	</select>

	<select id="findByParentidCount" resultType="java.lang.Integer">
		select count(1)
		from cms_resourcetype
		<where>
			parentid=#{parentid}
			and deleted=0
			and available=1
		</where>
	</select>

	<select id="checkForDelete" resultType="java.lang.Integer" parameterType="java.lang.String">
		select count(1) from cms_resourcetype where deleted=0 and parentid in ('${ids}')
	</select>

	<select id="getParentidsById" resultType="java.lang.String" parameterType="java.lang.String">
		select parentids from cms_resourcetype where id=#{id}
	</select>

	<update id="updateSort" parameterType="java.lang.String">
		update cms_resourcetype set sort=#{sort} where id=#{id}
	</update>

	<select id="getMaxSortByParentid" resultType="java.lang.Integer" parameterType="java.lang.String">
		select IFNULL(Max(sort),0) from cms_resourcetype where  parentid=#{parentid} and deleted=0
	</select>

	<select id="treeSelect"  resultType="java.util.Map">
		select <include refid="allColumns"/> FROM cms_resourcetype
		where
		deleted=0
		and available=1
		order by parentids,sort
	</select>
</mapper>
